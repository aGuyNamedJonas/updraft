{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna version --conventional-commits && lerna publish from-package --conventional-commits"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
