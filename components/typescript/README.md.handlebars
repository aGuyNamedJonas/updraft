# {{moduleName}}
{{#if templates.length}}
> Available Quickstart Templates (scroll down for instructions 👇🏾)  
{{#join templates ", "}}**{{name}}**{{/join}}  

{{/if}}
{{description}}

{{#if features.length}}
## Features
{{#features}}
**✓ {{headline}}**{{#if optional}} (optional){{/if}}{{#if description}} - {{description}}{{/if}}  
{{/features}}
{{/if}}

## Install
`npm install --save {{moduleName}}`

{{#if example}}
## Example
{{example}}
{{/if}}

## Quickstart Templates
{{#if templates.length}}
Get started with `{{moduleName}}` by using one of the available quickstart templates 👇🏼  
{{#join templates "\n  "}}
### {{name}}  
`$ updraft templates {{../moduleName}} {{name}}`  
{{description}}  

{{/join}}  
{{else}}
**No quickstart templates available for this component yet 😢**  
{{/if}}

<br/><br/>
**What are *updraft* templates?**  
> Quickstart templates for *updraft* components provide you with runnable examples for that component.  
Speed up your development workflow by getting started with templates.  

> List available templates: `updraft templates @updraft/aws-static-site` (replace `aws-static-site` with the componentof your choice)

> Install CLI: `npm i -g @updraft/cli`