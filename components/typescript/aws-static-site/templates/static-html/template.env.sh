#!/bin/bash
# Copy this to "env.sh" to configure this template:
# $ cp ./template.env.sh env.sh && chmod +x env.sh

# The ID of the AWS account you would like to deploy to (e.g. 717741335599)
export AWS_ACCOUNT_ID=""

# AWS Region of where to deploy to (e.g. eu-central-1)
export AWS_REGION=""

# The Route53 managed domain that you want to use (e.g. agnj.me)
export DOMAIN=""